import XCTest

@testable import FolderSync

final class P2PDiscoveryTests: XCTestCase {
    // mDNS is now always enabled by default, so these tests are no longer needed
    // Future tests can be added here for P2P discovery functionality
}
